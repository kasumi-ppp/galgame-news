# Legacy compatibility

`image_prescan_legacy.py` is the original single-file implementation. It is
kept unchanged so existing imports and commands remain usable. New work should
use the modular package in `src/galgame_news/` instead.
